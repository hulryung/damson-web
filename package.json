{
  "name": "damson-web",
  "version": "0.1.0",
  "private": true,
  "description": "Documentation site for Damson — the terminal built only for macOS",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "postbuild": "pagefind --site .next/server/app --output-path public/_pagefind"
  },
  "dependencies": {
    "next": "^15.1.6",
    "nextra": "^4.2.17",
    "nextra-theme-docs": "^4.2.17",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  },
  "overrides": {
    "zod": "4.1.12"
  }
}
